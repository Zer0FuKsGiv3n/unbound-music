# Unbound Music

Web-optimized streaming application built with React, TypeScript, and Wouter.

## Development Commands
- Install: `npm install`
- Type Check: `npx tsc --noEmit`
- Build: `npx vite build`
